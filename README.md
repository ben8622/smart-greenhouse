# Smart Greenhouse
## How to Run
1. `cd ~/smart-greenhouse`
2. Activate the virtual environment `source venv/bin/activate`
3. Run the flask app in develop mode on the local network `flask --app src/app.py run --host=0.0.0.0`